<!DOCTYPE html>
<html>
  <head>
    <title>React Isomorphic</title>
    <% if(NODE_ENV === 'production' || NODE_ENV === 'clientrender') { %>
      <link rel='stylesheet' href='/css/style.css' />
    <% } %>
  </head>
  <body>
    <div id="app"><%- html %></div>
    <script>
      window.__INITIAL_STATE_ = <%- state %>;
    </script>
    <script src="/js/app.js"></script>
  </body>
</html>
